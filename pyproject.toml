[project]
name = "tool-transcriber"
version = "0.1.0"
description = "Transcribe audio files to text via Whisper API"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
